# Voice-Recon
Voice recognition web application

#Setup
To install the dependencies run in your terminal ``pip install -r requirements.txt``

#Running the app
Set your terminal in the root directory, then: 
- ``cd VoiceRecon``  
- ``python manage.py runserver``
    
After this, go to http://localhost:8000/voiceRecorder/ and check that it is working.